# Data + analytics
openpyxl>=3.1          # read the source workbook (seed only)
pandas>=2.0
numpy>=1.26
pyyaml>=6.0
yfinance>=0.2.40       # market data: prices, dividends, fundamentals

# API backend (serves the React terminal in web/)
fastapi>=0.110
uvicorn[standard]>=0.29
